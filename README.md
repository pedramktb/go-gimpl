# gimpl

<img src="logo.png" alt="Logo" width="200"/>

gimpl stands for Go Generic Simple Implementations. It is a Go library that provides generic interfaces and automatated code generation for those interfaces which means there is no need to write boilerplate code, or use reflect heavy libraries such as `gorm.io/gorm`.